{
  "name": "github-webhook-handler",
  "version": "1.0.0",
  "description": "A pluggable server that handles github webhooks",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Bryant",
  "license": "ISC",
  "dependencies": {
    "fastify": "^2.7.1"
  }
}
